// String literal either in single or double quote both are equivalent .

const greeting = "Hello";
const wish = "Hi";

// Template string with backticks

const html = `<div>
  ${Math.random()}
</div>`;

document.getElementById("app3").innerHTML = html;
